spring:
  application:
    name: LogEYE
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL # H2를 PostgreSQL 모드로 실행
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

logging:
  level:
    org.hibernate.SQL: debug

jwt:
  secret-key: aGVsbG8td29ybGQtcHJvamVjdC1sb2dleWUtc2VjcmV0LWtleS1mb3Itand0LXNlY3VyaXR5LWhzaDI1Ni1hbGdvcml0aG0K
  access-token-expiration-millis: 1800000
  refresh-token-expiration-millis: 604800000